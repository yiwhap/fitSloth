[project]
name = "fitsloth"
version = "0.1.0"
description = "Image-to-image food search using CLIP embeddings"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "numpy==1.26.4",
    "Pillow==10.3.0",
    "torch==2.8.0",
    "transformers==4.44.2",
]

[tool.uv]
package = false
